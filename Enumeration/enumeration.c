// Enumeration or enum
#include <stdio.h>

enum Bool{No, Yes};

int main(){

  enum point {X = 10, Y};

  enum Bool a;

  enum point b;

  a = Yes;

  b = Y;

  printf("\n a : %d",a);

  printf("\n b : %d",b);

 return 0;

}
